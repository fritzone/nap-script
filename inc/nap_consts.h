#ifndef NAP_CONSTS_H
#define NAP_CONSTS_H

/** Means the execution was succesful */
const int NAP_EXECUTE_SUCCESS = 1;

/** Means the execution was not successful */
const int NAP_EXECUTE_FAILURE = 0;

/** Means the requested variable was found */
const int NAP_VARIABLE_FOUND     = 1;

/** Means the requested variable was not found */
const int NAP_VARIABLE_NOT_FOUND = 0;

/** Represents that there is no such value*/
const int NAP_NO_VALUE = 0x0BADF00D;

#endif // NAP_CONSTS_H
